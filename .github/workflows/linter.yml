---
name: "Lint Code Base"

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  linter:
    name: "Lint Code Base"
    uses: pacroy/gh-common-workflows/.github/workflows/wf_linter.yml@v1
    secrets: inherit
