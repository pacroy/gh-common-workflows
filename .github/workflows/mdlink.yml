name: Check Markdown Links

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  markdown-link-check:
    name: Check Markdown links
    uses: pacroy/gh-common-workflows/.github/workflows/wf_mdlink.yml@v1
    secrets: inherit
